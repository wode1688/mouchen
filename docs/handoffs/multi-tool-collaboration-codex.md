# 多电脑与多 AI 工具源码协作

- 执行工具：Codex
- 分支：`codex/cloud-relay-sync`
- 上一份已完成提交：`266dd94e2f511d0e5f716628622cfbfbcd61984f`
- 记录日期：2026-09-10
- 本记录创建时状态：协作文件已在本地准备，GitHub 授权及远端发布待核实

## 用户目标

在一个 GitHub 仓库里管理 AI 替身源码，支持多台电脑及 Codex、Claude Code、Hermes 等工具修改、接续工作和保留历史。临时中转继续按小容量、短期保留的方向维护。

## 已完成

- 上一提交加入可选临时中转、电脑伴随程序、部署示例、隔离测试和多电脑开发文档。
- 新增统一 `AGENTS.md` 与 Claude Code 引用入口。Hermes 按已核对的官方规则读取共享 `AGENTS.md`。
- 新增 GitHub 任务与 PR 模板、交接规范，以及创建独立任务目录和分支的 Python 辅助脚本。
- 辅助脚本支持任意合法工具名，默认获取远端更新；拒绝覆盖已有任务，不改变原工作目录中的未提交工作。

## 验证

- 临时中转上一提交：Windows / Python 3.12 下 22 项测试与根目录虚拟演示通过。
- 协作脚本：Windows / Python 3.12 下 6 项隔离测试通过，使用临时虚拟 Git 仓库。
- 官方项目入口方式已核对；未在本机实际启动 Claude Code 或 Hermes 验证会话加载。
- GitHub Actions 配置已写入。远端测试尚未运行，不能据此宣称 Linux 或 Python 3.11 已通过。

## 剩余工作与下一步

1. 完成 GitHub 授权，推送本分支，创建关联 Issue 和 PR，回读远端提交及 CI 结果。
2. 在使用 Claude Code、Hermes 的电脑打开独立任务目录，核对实际读到的规则和交接内容。
3. 按具体需求实现手机端接入和原程序中的本地处理；伴随归档目前不会自动导入原应用数据库。
4. 若需要自动派工，再单独接入各工具的执行接口、任务领取和失败恢复。当前文件不提供跨工具自动调度、全局锁或自动合并。

## 接手注意

从 Git 实际读取本记录所属提交，并获取远端最新状态。早期“授权待完成”的记录可能在上传后过时，应以真实远端结果为准。个人配置和服务凭据通过各设备的私有配置管理，不包含在源码及本交接记录中。
