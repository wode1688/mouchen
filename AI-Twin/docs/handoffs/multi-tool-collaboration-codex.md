# 多电脑与多 AI 工具源码协作

- 执行工具：Codex
- 分支：`codex/cloud-relay-sync`
- 上一份已完成提交：`4d6995c6f9b631043a0279308851a8d47437cc65`
- 记录日期：2026-09-10
- 关联任务：[Issue #1](https://github.com/wode1688/mouchen/issues/1)
- 本次目录调整状态：应用文件已在本地迁入 `AI-Twin/`，7 项协作测试和应用虚拟演示通过；已核实账号有仓库写权限，远端分支和 CI 结果仍需回读确认

## 用户目标

在一个 GitHub 仓库里管理 AI 替身源码，支持多台电脑及 Codex、Claude Code、Hermes 等工具修改、接续工作和保留历史。按用户最新要求，在 `AI-Twin/` 专门目录统一放置应用源码。临时中转继续按小容量、短期保留的方向维护。

## 已完成

- 提交 `266dd94` 加入可选临时中转、电脑伴随程序、部署示例、隔离测试和多电脑开发文档。
- 新增统一 `AGENTS.md` 与 Claude Code 引用入口。Hermes 按已核对的官方规则读取共享 `AGENTS.md`。
- 新增 GitHub 任务与 PR 模板、交接规范，以及创建独立任务目录和分支的 Python 辅助脚本。
- 辅助脚本支持任意合法工具名，默认获取远端更新；拒绝覆盖已有任务，不改变原工作目录中的未提交工作。
- 现有应用源码及上述组件已迁入 `AI-Twin/`；共同规则、贡献说明、安全政策、许可证及 `.github/` 留在仓库根目录。当前交接路径为 `AI-Twin/docs/handoffs/multi-tool-collaboration-codex.md`，应用运行命令从 `AI-Twin/` 执行。

## 验证

- 目录调整前的临时中转提交：Windows / Python 3.12 下 22 项测试与当时应用根目录的虚拟演示通过；演示现位于 `AI-Twin/examples/demo.py`。
- 目录调整后的协作脚本：本地 7 项隔离测试通过，使用临时虚拟 Git 仓库；`AI-Twin/` 应用目录虚拟演示通过。
- 官方项目入口方式已核对；未在本机实际启动 Claude Code 或 Hermes 验证会话加载。
- GitHub Actions 配置已写入。远端测试尚未运行，不能据此宣称 Linux 或 Python 3.11 已通过。

## 剩余工作与下一步

1. 完成其余受影响组件的路径迁移检查，补充本次实际验证结果。
2. 推送本分支，更新关联 Issue 并创建或更新 PR，回读远端提交及 CI 结果。
3. 在使用 Claude Code、Hermes 的电脑打开独立任务的仓库根目录，核对实际读到的规则和 `AI-Twin/docs/handoffs/` 中的交接内容。
4. 按具体需求实现手机端接入和原程序中的本地处理；伴随归档目前不会自动导入原应用数据库。
5. 若需要自动派工，再单独接入各工具的执行接口、任务领取和失败恢复。当前文件不提供跨工具自动调度、全局锁或自动合并。

## 接手注意

从 Git 实际读取本记录所属提交，并获取远端最新状态。早期“授权待完成”的记录可能在上传后过时，应以真实远端结果为准。个人配置和服务凭据通过各设备的私有配置管理，不包含在源码及本交接记录中。

本次整理的来源为现有仓库源码及后续修改，尚未通过可复现构建确认其与已安装的 `AI-Twin.exe` 完全一致。目录迁移和源码上传不代表安装程序或正在运行的服务器已更新。
