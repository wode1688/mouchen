# 任务交接记录

每个任务一份 `<task>-<tool>.md`，创建新任务时可由 `tools/ai_workspace.py` 生成。换工具接手同一任务时继续更新原文件，并记下新执行者。

记录目标、分工范围、关联 Issue、分支、上一份已完成提交、已完成工作、实际验证、剩余工作和下一步。当前记录所属的提交从 Git 历史读取，不预填尚不存在的提交号。

只记录项目进度和虚拟示例。不要写入私人路径、真实设备标识、服务地址、密钥、日志或聊天全文。账号和运行配置不通过交接文件传递。
